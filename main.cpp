#include "shared_types.h"

int main()
{
	//ent_chunk<int> integers;
	//const bitset_512& the_bitset = integers.getStates();
	//std::cout << the_bitset.get(0);
	//integers.insert(42);
	//std::cout << the_bitset.get(0);


}